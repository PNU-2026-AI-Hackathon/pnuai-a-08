# 서로서가 Android 작업 행동 강령

## 1. 목적

이 문서는 기능 명세서가 아니라 **구현 작업 방식과 판단 순서를 통일하기 위한 행동 강령**이다.

## 2. 최우선 원칙

화면(UI), 트리거(Trigger), 화면 이동, 화면 내부 상태 변화, 데이터 계층을 분리하여 판단한다.

에이전트는 구현 전에 반드시 현재 프로젝트 구조를 먼저 확인한다. 특히 다음 항목을 우선 확인한다.

- 기존 `Screen`/`Activity` 존재 여부
- 기존 `Component` 재사용 가능 여부
- `docs/page_interactions.md`에 기록된 현재 화면 흐름
  - 파일이 아직 없다면, 화면 이동 흐름을 추가하거나 새 `Screen`을 생성하는 작업 시 생성 필요 여부를 먼저 확인한다.
- 기존 `Repository`/`Data` 계층 구조
- 이미 존재하는 유사 기능 여부
- 현재 프로젝트가 `Activity` 기반 이동인지, Navigation Compose 등 기존 내비게이션 구조를 사용하는지 여부

기존 구조와 충돌하거나 중복되는 구현은 우선적으로 피한다.

> **추가 보강 지침:** 원문은 `ActivityNavigator` 사용을 전제로 예시를 들고 있다. 그러나 실제 프로젝트에 이미 다른 내비게이션 방식이 구축되어 있다면, 새로운 방식으로 강제 교체하거나 별도 `Activity`를 남발하지 않는다. 기존 이동 구조를 확인한 뒤 그 방식에 맞춰 구현 계획을 세운다.

## 3. 구현 전 작업 판단 순서

1. 작업의 시작점이 되는 화면을 확인한다.
2. 해당 화면에서 기능을 실행시키는 트리거 UI 요소를 확인한다.
3. 기능의 동작 유형을 분류한다.
4. 기능 구현 판단 규칙을 참고하여 구현 계획을 수립한다.
5. 수정 파일 범위를 확정한다.
6. 작업 계획을 먼저 출력하고, 확인 후 구현을 진행한다.

## 4. 구현 전에 반드시 분석할 항목

새로운 기능 요청을 받으면 즉시 코드를 수정하지 않고, 먼저 다음 항목을 분석한다.

- 트리거 요소
- 화면 이동 여부
- 현재 `Screen` 내부 상태 변경 여부
- DB/API 연관 여부
- 수정 파일 범위
- 새 파일 생성 필요 여부
- `docs/page_interactions.md` 수정 필요 여부

불명확한 부분이 존재하면 구현 전에 질문을 통해 확인한다.

## 5. 구현 과정의 필수 준수 사항

- 기존 컴포넌트 또는 유사 구현을 재사용할 수 있는지 먼저 확인한다.
- 새 화면 생성 여부와 현재 화면 내부 상태 처리 여부는 `docs/rules/IMPLEMENTATION_DECISIONS.md`를 기준으로 판단한다.
- 화면 이동 흐름을 추가하거나 수정하면 `docs/rules/PAGE_INTERACTIONS_GUIDE.md`에 따라 `docs/page_interactions.md`에 기록한다.
  - 해당 파일이 아직 없으면 임의로 생성하지 않고, 작업 계획에서 생성 필요 여부를 먼저 확인한다.
- 파일 생성·수정 범위는 `docs/PROJECT_STRUCTURE.md`의 역할 분리 기준을 따른다.
- `MainActivity`와 앱 루트 Composable은 앱 진입점, 테마, 내비게이션 연결 같은 최상위 조립 역할로 제한한다.
- 요청 형식 및 계획 출력 형식은 `docs/templates/CODEX_REQUEST_TEMPLATE.md`를 따른다.

## 6. 금지 사항

- 기존 `Screen` 내부에 다른 페이지의 UI를 억지로 구현하지 않는다.
- 필요 없는 `Activity`/`Screen`을 새로 만들지 않는다.
- `Activity`/`Screen` 내부에 목(mock) 데이터를 직접 하드코딩하지 않는다.
- `MainActivity` 안에 화면 UI, 재사용 컴포넌트, 데이터 모델, 목(mock) 데이터 목록을 몰아넣지 않는다.
- `Composable` 내부에서 DB/API를 직접 호출하지 않는다.
- 화면 이동 시 기존 내비게이션 계층을 우회하여 임의로 이동 로직을 직접 작성하지 않는다.
  - 기존 구조가 `ActivityNavigator`라면 `Intent`를 화면에서 직접 생성하지 않고 `ActivityNavigator`를 사용한다.
- 기존 UI 디자인과 레이아웃 구조를 임의로 크게 변경하지 않는다.
- 다른 기능 영역의 파일을 임의로 수정하지 않는다.
- `docs/page_interactions.md` 기록 또는 생성 필요 여부 확인 없이 새 `Screen`을 생성하지 않는다.

## 7. 문서 간 우선순위

1. 현재 프로젝트에 실제로 존재하는 구조와 코드
2. `docs/page_interactions.md`에 기록된 현재 화면 흐름
   - 파일이 아직 없는 경우에는 실제 코드의 기존 내비게이션 구조를 우선 확인한다.
3. 이 행동 강령과 세부 규칙 문서
4. 예시 문서

예시는 판단을 돕기 위한 참고 자료이며, 실제 프로젝트 구조와 충돌할 경우 그대로 복사하여 적용하지 않는다.
